# Предложение: [Название функционала / задачи]

> **Дата**: ГГГГ-ММ-ДД  
> **Статус**: На рассмотрении / Утверждено  
> **Целевой модуль**: `src/app/modules/[название_модуля]`

---

## 1. Концептуальный фильтр (Сверка с `docs/ABSTRACT.md`)
- **Доменные сущности**: *Перечислить затрагиваемые сущности (например, Room, User, Device, StreamSession, DeviceShadow)*
- **Модель прав $P(S, A, R, C)$**:
  - **Субъект ($S$)**: *Роли, инициирующие действие (Guest, Operator, Admin, System)*
  - **Действие ($A$)**: *Тип операции (Read, Control, Takeover, Modify)*
  - **Ресурс ($R$)**: *Целевой объект воздействия*
  - **Контекст ($C$)**: *Условия доступа (активная комната, валидный токен, статус устройства)*
- **Инварианты**: *Как соблюдаются SSOT, Fail-Safe состояние, On-Demand стриминг и Device Shadow*

---

## 2. Сквозная трассировка (Сверка с `docs/ARCHITECTURE.md`)
- **E2E Data Flow**: `UI Component (Signals) -> Transport -> Gateway/Controller -> Guard -> Application Service -> Driver/ORM -> DB/Hardware`
- **Задействованные протоколы**: *(WSS Socket.IO, WebRTC WHEP, REST API, UDP MAVLink/CRSF, Serial)*
- **Точки отказа и обработка сбоев**: *(таймауты, потеря связи, реконнект)*

---

## 3. Цели (Goals)
*Что конкретно БУДЕТ реализовано в рамках этой задачи:*
- [ ] Цель 1: ...
- [ ] Цель 2: ...

---

## 4. Не-цели (Non-Goals)
*Что явно НЕ входит в скоуп данной задачи (защита от расползания рамок):*
- ❌ Не-цель 1: ...
- ❌ Не-цель 2: ...

---

## 5. Вариант со слоем абстракции (Extensibility & Abstraction Layer)
*Обязательное описание изолирующей прослойки для масштабируемости в будущем:*
- **Интерфейс / Порт**: `I[EntityName]Port` / `I[EntityName]Repository` / `I[DriverName]Adapter`
- **Текущая реализация (Default Provider)**: например, локальный адаптер / текущий транспорт.
- **Точки будущего масштабирования**: возможность замены драйвера/БД/транспорта (например, переключение с REST на WebSocket, с SQLite на PostgreSQL, смена видеосервера) без изменения бизнес-логики.

---

## 6. Риски и альтернативы
*Потенциальные сложности, влияние на существующий функционал и рассмотренные альтернативные подходы.*

